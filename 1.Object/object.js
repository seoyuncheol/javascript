var memberArray = ["kim", "park", "seo"];

// 배열 CRUD

// 객체란 서로 연관된 변수와 함수를 그룹핑하여 이름을 붙인 것.
var memberObject = {
  manager: "kim",
  developer: "park",
  designer: "seo"
};

// 객체 CRUD

delete memberObject.manager;

console.log("memberObject.manager", memberObject.manager);
